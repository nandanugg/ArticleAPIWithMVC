<!-- 
Modularized modal, it requires:
  1. modal id, as reference to help bootstrap show correct modal (id)
  2. title (title)
  3. form action (action)
  4. textarea content (text)
to be working properly
-->
<div class="modal fade" id="<%= id %>" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">
          <%= title %>
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <form method="POST" action="<%= action %>">
        <div class="modal-body">
          <label for="article" class="col-form-label">Insert article</label>
          <textarea class="form-control" name="content"><%= text %> </textarea>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-primary">Send</button>
        </div>
      </form>
    </div>
  </div>
</div>
